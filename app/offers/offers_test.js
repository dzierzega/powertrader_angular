'use strict';

describe('myApp.offers module', function() {

  beforeEach(module('myApp.offers'));

  describe('offers controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var OffersCtrl = $controller('OffersCtrl');
      expect(OffersCtrl).toBeDefined();
    }));

  });
});